import { OnlineUsers } from "./onlineUsers"

export const Header = () => {
    return (
        <header>

            <h1 className="text-3xl">Título da página</h1>
            <OnlineUsers/>
        </header>
    )
}